[pytest]
markers =
    unit: tests sin API (rápidos, corren en CI siempre)
    integration: tests con API real (lentos, corren antes de merge)
    eval: evaluación de calidad con LLM-as-judge
asyncio_mode = auto
testpaths = tests
